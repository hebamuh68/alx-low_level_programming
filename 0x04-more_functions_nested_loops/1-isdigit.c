#include "main.h"
/**
 * Write a function that checks for a digit (0 through 9).
*/
int _isdigit(int c)
{
if (c >= 'A' && c <= 'Z')
{
_putchar(0);
}
else if (c >= 'a' && c <= 'z')
{
_putchar(0);
}
else
{
_putchar(1);
}
}
